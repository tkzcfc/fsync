

```
  fsync.exe COMMAND {OPTIONS}

    file sync

  OPTIONS:

      commands
        sync                              synchronize folder content
        copy                              copying files or folders
      arguments
        -h, --help                        help
        --log_level=[level]               The log level
```
